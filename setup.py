from setuptools import setup

setup(
    name = "statstock",
    version = "1.0",
    packages = ["statstock"],
    install_requires = ["numpy"],
    author = "Luke Siemens",
    author_email = "luke@lsiemens.com"
)
